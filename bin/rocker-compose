#!/bin/sh
# Run rocker-compose inside a container
exec docker run -it --rm \
    -v /var/run/docker.sock:/var/run/docker.sock \
    -e "TARGET=$(pwd)" \
    -v $(pwd):$(pwd) \
    bwstitt/rocker-compose \
    "$@"
