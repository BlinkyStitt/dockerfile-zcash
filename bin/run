#!/bin/sh

# exit on error
set -e

# run from inside the application's HOME directory
DIR="$(cd "$(dirname "$0")" && pwd -P)"
cd "$DIR/.."
pwd

echo "Updating images..."
docker pull bwstitt/rocker-compose:latest
./bin/rocker-compose pull

echo "Running containers..."
./bin/rocker-compose run "$@"
